# 0.6.68 meeting-note reliability

## Regressions

`bash scripts/test-meeting-summary.sh` exercises production summary normalization, aggregation, source evidence, transcript segmentation, overlap removal, and saved-text restoration.

Observed red cases before their fixes:

- Valid semantic actions replaced with conversational keyword matches.
- Decimal versions split into multiple transcript entries.
- Filler differences prevented removal of matching cross-source echo.
- Short acknowledgments were treated as sufficient echo evidence.
- No review marker for material following a late exchange of farewells.

Passing checks include action lists longer than six, instruction-leak rejection, all 100 parts retained during aggregation, negation differences retained, post-meeting text preserved, existing timestamped formats restored, original transcript body preserved byte-for-byte when notes are refreshed, and evidence restricted to existing source entries.

## Full local evaluation

The user-authorized private meeting stays outside Git and Linear. No private fixture, summary, or recording is committed.

The full-meeting evaluation exposed a model guardrail error and a final context overflow in earlier candidates. Neither may be hidden by an opening-utterance fallback. The revised pipeline uses Apple's supported content-transformation model configuration, bounded per-part processing, separate structured follow-up extraction, and bounded overview synthesis. Key points and actions do not pass through an overflowing final condensation request.

An intermediate candidate completed but failed manual quality review: it promoted recommendations into tasks, omitted commitments, and reconstructed source quotations. It was not published. Task evidence now resolves to actual transcript entries, copied verbatim by application code rather than reconstructed by the model.

## Scope and limitations

This does not repair past ASR substitutions or restore words absent from the recording/transcript. Cross-source echo handling remains conservative and requires real audio evaluation for a measured accuracy claim. Farewell detection is explicitly a review hint, not an automatic trimming rule. Generated task interpretation, owners, and dates still require user review; source evidence is available to check them.

## Publication gate: not passed

Further Apple-model evaluation still promoted anecdotes and architectural descriptions into tasks and sometimes selected an irrelevant evidence entry. Source-entry existence is not proof that it supports the generated task. A local `gpt-oss:20b` whole-meeting benchmark produced a clearer overview but invented deadlines despite instructions not to do so (156 seconds; 21,342 prompt tokens; 992 output tokens; model unloaded afterward). Neither output is approved for release.

Signed/notarized candidate archives are internal QA artifacts only. They must not be described as a published or quality-verified release. The installed/public 0.6.67 remains unchanged pending a successful quality gate. The user's history was backed up before potential installed-app testing and has not been edited by this work.

A stricter whole-meeting, quote-only `gpt-oss:20b` follow-up extraction trial terminated with `UND_ERR_HEADERS_TIMEOUT` and produced no output. This is not an accuracy result or a passing alternative. Ollama's model inventory was empty afterward, confirming no evaluation model remained loaded.

Installed-app and final artifact results must be recorded after verification, not inferred from successful unit tests or notarization.

## September 22: acoustic timing and packaged GPU failure

The installed 0.6.67 Whisper path failed Metal initialization because its bundled `ggml-metal.metal` referenced a missing `ggml-common.h`. It silently fell back to CPU. This is an observed packaging defect, not proof of the cause of a tester's earlier overheating report (their selected model is unknown).

The packaging script now inlines the matching header and compiles the actual generated Metal source as a build gate. Archive verification applies the same check. The installed resource failed the new check; the prepared resource compiled 225 kernels on Apple M4 Pro. The rebuilt ad-hoc candidate's executable also initialized Apple M4 Pro GPU without a resource-path override and transcribed a local 18-second excerpt. The installed application was not overwritten.

Meeting sentence timing now maps Parakeet word spans to sentences rather than distributing sentences uniformly across a chunk. Text/timing mismatches and invalid timings fall back to the previous estimate. Synthetic pause, mismatch, NaN, and out-of-chunk tests pass. A real full-recording diagnostic produced timed sentence output locally; this is not a word-accuracy or speaker-accuracy certification. The preview, chunk coverage, and transcript reader regression scripts also pass.

A local Whisper Large v3 Turbo whole-file pass over 58:29 of mixed Zoom audio completed in 214.73 seconds (18.83 user / 3.47 system CPU seconds; approximately 2.74 GB peak memory footprint). A candidate build ran during part of that trial, so this is not a controlled performance comparison. Output contained only 2,688 whitespace-delimited words, severe repetition, and omissions. It fails the quality gate. The model remains an evaluation download, not a changed default. A chunked meeting-path comparison is evaluated separately.

Another local structured `gpt-oss:20b` extraction trial selected anecdotes as tasks and hit its generation limit before completing all parts. It was not integrated. No private meeting content has been uploaded to a cloud model or committed. Summary quality remains unresolved; publication remains blocked.

The separate Whisper chunked diagnostic completed all 124 pause-aware chunks in 168.49 seconds (16.54 user / 2.18 system CPU seconds; approximately 2.43 GB peak memory footprint), producing 9,981 whitespace-delimited words. It is substantially more complete than the whole-file invocation and preserves several difficult phrases, including a technical proper noun that Parakeet split into two words. It still disagrees with another transcript on an important negation and emits short text during leading silence. Word count is not accuracy, and this mixed-audio diagnostic is not a live two-track meeting test. No human-corrected reference or measured WER exists. Do not claim Granola parity or change the default model on this evidence alone.

Before publication, failure recovery also needs installed-app testing: a thrown finalization error must not leave the user without a recoverable recording or transcript. Audio saving is currently optional. Exposing failed chunks is safer than silently labeling partial text complete, but is not by itself a recovery feature.

## User-authorized OpenAI summary evaluation

With explicit permission for transcript-only OpenAI processing and existing-key reuse, two `gpt-5.5-2026-04-23` high-reasoning evaluations ran with `store: false`. No audio or competing summary was submitted. The original transcript's embedded summary was excluded. Private inputs/outputs remain outside this repository; credentials were not copied into Kiki.

First pass (120.684 seconds) recovered all ten manually selected substantive follow-up categories, but overclassified suggestions/advice as commitments. Of 85 quoted evidence bullets, 82 exactly matched their cited lines. A stricter second pass (88.338 seconds) retained seven checklist categories as confirmed actions, moved two to proposals/open questions, and dropped one research follow-up while retaining its topic. Of 24 quoted evidence bullets, 23 matched. This is a non-blinded, single-meeting manual checklist, not an exhaustive recall measure or cross-meeting benchmark.

Conclusion: a stronger summarizer materially improves coverage on the same transcript, but naive generate-then-strict-audit still fails the quality gate. Preserve tentative follow-ups separately from commitments and proposals; resolve source quotations in code from validated entry IDs; evaluate semantic support and recall together. Cloud processing must remain explicit opt-in if implemented. No cloud integration, installed-app update, or public release was performed by this evaluation.

## Local alternatives screen — September 22–23

The subsequent evaluation used local Ollama only, on an M4 Pro with 48 GB RAM. No meeting content was sent to cloud inference. The same private transcript, excluding its existing notes, supplied 866 source entries. Private outputs and source evidence remain outside Git and Linear.

| Configuration | Elapsed | Outcome |
|---|---:|---|
| Qwen3.5 4B Q4_K_M, whole meeting | 189 s | Valid structure; omissions, role errors, negation reversal |
| Qwen3-Coder 30B Q4_K_M | 518 s | Valid structure; omissions, unsupported task/evidence associations |
| Devstral 24B Q4_K_M | 600 s limit | No usable output; no accuracy result |
| gpt-oss 20B MXFP4 | 337 s | Generation limit; incomplete JSON and invalid evidence IDs |
| Qwen3.5 4B, eight bounded sections | 308 s | All sections valid; false tasks, duplicates, unsupported deadline; no final consolidation |
| Ornith 1.5 9B BF16, bounded evidence | 499 s | Valid structure; lost follow-ups, wrong participant, incomplete evidence |

Qwen2.5-Coder 14B was interrupted by an Ollama service shutdown; it has no accuracy result. An earlier 30B trial hit the client's header timeout and was rerun after fixing the client. These transport/service failures must not be scored as model accuracy failures.

Whole-meeting settings were 32K context, 5K output budget, temperature zero, seed 42. The final Ornith and section trials hardened evidence constraints, so they are not strict same-configuration rankings. Sampling was not individually optimized to publisher recommendations. One non-blinded meeting cannot establish universal model quality or Granola parity.

No candidate passed the semantic quality gate. Existing source IDs and valid JSON are insufficient: tasks, owners, timing, and supporting passages must agree. Local model/context allocations ranged from roughly 3.4 GB for the section trial to 21.8 GB for the 30B trial. These are not minimum RAM requirements, fan measurements, or clean-room performance results. A separate process sampler replaces invalid zero CPU/RSS values from the initial sampler.

All evaluation models were unloaded afterward. No app source, default model, installed application, landing page, or public release changed. Keep the issue in progress. Next evaluation should test a general-purpose local model with recommended sampling, bounded evidence and source-window verification, plus a human-reviewed multi-meeting reference; this is not yet implemented or validated.
